# Only needed for production builds where the game and the API are served
# from different origins. Leave unset for local dev — the Vite dev server
# proxies /api to the server/ project (see vite.config.ts), so requests stay
# same-origin and VITE_API_URL defaults to '' (relative /api/... calls).
VITE_API_URL=https://api.example.com
