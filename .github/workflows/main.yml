name: Build installable Android APK

on:
  workflow_dispatch:
    inputs:
      api_url:
        description: Public HTTPS URL of the UNDRAL API
        required: false
        type: string
      app_hostname:
        description: Virtual app origin allowed by the backend
        required: true
        default: app.undral.game
        type: string

jobs:
  android-debug-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      VITE_API_URL: ${{ inputs.api_url || secrets.API_URL }}
      CAPACITOR_HOSTNAME: ${{ inputs.app_hostname }}

    steps:
      - name: Check out project
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Install JavaScript dependencies
        run: npm ci --ignore-scripts || npm install --ignore-scripts

      - name: Validate mobile API URL
        shell: bash
        run: |
          if [[ ! "$VITE_API_URL" =~ ^https://[^[:space:]]+$ ]]; then
            echo "Enter an HTTPS API URL or create the API_URL repository secret." >&2
            exit 1
          fi

      - name: Verify and bundle the game
        run: |
          npm run check:authority
          npm test -- --run
          npm run build
          npx cap sync android

      - name: Build installable debug APK
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew clean assembleDebug --no-daemon

      - name: Verify APK
        run: |
          test -s android/app/build/outputs/apk/debug/app-debug.apk
          ls -lh android/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: undral-android-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
