# Build-time endpoint used by the installed app.
# HTTPS is required for production authentication cookies and WebSockets.
VITE_API_URL=https://api.undral.game

# Optional build-time Capacitor virtual origin. Keep it under the same
# registrable domain as the API when possible (for example app.undral.game).
CAPACITOR_HOSTNAME=app.undral.game
